server.port : 9006

spring:
  application:
    name: dding-board-board-service
  datasource:
    url: jdbc:mysql://localhost:3306/board
    username: root
    password: 1001
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
  date:
    redis:
      host: 127.0.01
      port: 6379

  kafka:
    bootstrap-servers: 127.0.0.1:9092